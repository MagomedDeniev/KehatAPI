framework:
    rate_limiter:
        forgot_password_ip:
            policy: fixed_window
            limit: 10
            interval: '15 minutes'

        forgot_password_email:
            policy: fixed_window
            limit: 3
            interval: '15 minutes'
